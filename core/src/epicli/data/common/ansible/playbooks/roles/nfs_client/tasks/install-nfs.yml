---
- name: Install NFS client packages
  package:
    name: "{{ packages[ansible_os_family] }}"
    state: present
  vars:
    packages:
      Debian:
        - nfs-common
        - gssproxy
      RedHat:
        - nfs-utils
        - gssproxy
